using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HBound : MonoBehaviour
{
    /*
    private void OnCollisionEnter2D(Collision2D collisionData)
    {
        OnTriggerEnter2D(collisionData.collider);
    }

    private void OnTriggerEnter2D(Collider2D colliderData)
    {
        Rigidbody2D collidedObject = colliderData.gameObject.GetComponent<Rigidbody2D>();
        HeroBehavior heroScript = colliderData.gameObject.GetComponent<HeroBehavior>();
        if (collidedObject != null)
        {
            Debug.Log("Wall!");
            // if (heroScript.direction != null)
            //{
            // Debug.Log(heroScript.direction);
            // Vector3 newDirection = heroScript.direction;
            // newDirection.y = -newDirection.y;
            //collidedObject.transform.rotation = Quaternion.LookRotation(new Vector3(0,-1,0), Vector3.forward);
            // }

            // subtract health from the player
            //float y = heroScript.direction.y;
            //heroScript.changeHDirection();
            //transform.rotation = Quaternion.AngleAxis(angle, Vector3.forward);
            

        }
    }*/
}